#set par(first-line-indent: 0em)
//#set text(font: "Open Sans", size: 11pt)

#set heading(outlined: false, numbering: none)
#show heading: set text(
  weight: "regular"
)

#show heading.where(level: 2): set text(size: 16pt)
#show heading.where(level: 3): set text(size: 14pt)
#show heading.where(level: 4): set text(size: 12pt)
#show heading.where(level: 5): set text(size: 12pt)

=== Comments about Miro (CAM)

*CAM-0*: hard to keep track of keys

*CAM-1*: hard to answer question without calculating

*CAM-2*: hard to calculate averages

=== Comments from Participants (CFP)

*CFP-0*: unsure where pathfinding fails

*CFP-1*: bigger node titles would be nice

*CFP-2*: sum of values would be nice

*CFP-3*: baseline in diagrams not at 0

*CFP-4*: hard to differentiate node - background

*CFP-5*: would be nice to see kind of lock via hover

*CFP-6*: overlapping edges difficult

*CFP-7*: unhappy with zoom

*CFP-8*: unsure whether red edges are on path or locked in general

*CFP-9*: y axis not at 0

=== Clarifications Given (CLG)

*CLG-0*: enemies respawn (miro, first)

*CLG-1*: same symbol for all lock types

*CLG-2*: same symbol for all lock types

*CLG-3*: enemies respawn (miro, first)

*CLG-4*: same symbol for all lock types

*CLG-5*: enemies respawn (miro, first)

*CLG-6*: no pathfinding beyond locked edges

*CLG-7*: same symbol for all lock types

*CLG-8*: same symbol for all lock types

=== Informal Feedback (IFB)

*IFB-0*: nice tool to work with

*IFB-1*: movement/unclear

*IFB-2*: would use again

*IFB-3*: color coding for which nodes were reachable when path not found

*IFB-4*: zoomed out view

*IFB-5*: coole tools/functinality, potential mit mehr usability

*IFB-6*: missing: comparing multiple paths

*IFB-7*: usability (edge editing without toolbar e.g. double click, more descriptive lock symbols)

*IFB-8*: idea: setting color of nodes

*IFB-9*: from protocol: not sure where section A ends (obs: diagram ticks only every second node on path)

*IFB-10*: reasonably performant

*IFB-11*: indicate state of nodes during pathfinding (available keys etc)

*IFB-12*: interesting tool, but a learning curve (familiarization, miro more simple)

*IFB-13*: text size small in nodes when zoomed out

*IFB-14*: lock symbols in pixe

*IFB-15*: select individual nodes for diagrams

*IFB-16*: fix lock icons

*IFB-17*: improve zoomed out overview

=== Observation from Protocol (OFP)

*OFP-0*: successive pathfinding

*OFP-1*: successive pathfinding

*OFP-2*: successive pathfinding

*OFP-3*: successive pathfinding

*OFP-4*: sub-paths incrementally

*OFP-5*: successive pathfinding

*OFP-6*: successive pathfinding

*OFP-7*: successive pathfinding

*OFP-8*: successive pathfinding

*OFP-9*: successive pathfinding
