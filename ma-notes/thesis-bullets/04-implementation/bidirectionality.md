
- deemed important: especially on lower level modeling (e.g. one node = one room), going back and forth *using the same lock* is a common scenario #citation-needed

### Functionality

- users should be able to set uni- or bi-directional edges

### Implementation

- no native support in VueFlow for bidirectional edges -> add flag to `PxChartEdge` data model
